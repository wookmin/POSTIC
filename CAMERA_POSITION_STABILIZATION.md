# 카메라 위치 이동 대응 및 자세 인식 안정화

## 목적

노트북 카메라를 로봇 또는 사용자 기준으로 앞뒤·좌우로 옮겨도 자세 판정이 크게 흔들리지 않도록 한다. 수동 캘리브레이션은 최소화하고, 카메라 시점이 바뀌는 동안 잘못된 로봇 반응이 발생하지 않게 하는 것이 우선이다.

## 현재 구현의 대응 범위

현재 `src/perception/posture_features.py`는 화면상의 2D 랜드마크를 사용한다. 어깨 너비와 골반 너비를 신체 크기 기준으로 사용하므로 사람과 카메라 사이의 거리가 변하는 상황에는 어느 정도 대응한다.

### 앞뒤 거리 이동

카메라가 사람을 계속 정면에서 바라보면서 가까워지거나 멀어지는 경우에는 어깨 너비 기반 정규화가 작동한다.

- 사람의 화면 크기가 바뀌어도 세로 간격을 어깨·골반 너비로 나눈다.
- 작은 거리 변화는 별도 캘리브레이션 없이 처리할 수 있다.
- 너무 가까워져 어깨·골반이 화면 밖으로 나가면 `unknown`으로 처리될 수 있다.

### 좌우 측면 이동

카메라를 좌우로 옮기더라도 렌즈가 계속 사용자를 정면으로 향하고 시점 변화가 작으면 사용할 수 있다. 그러나 카메라가 사용자를 비스듬히 바라보게 되면 현재 2D 지표의 신뢰도가 낮아진다.

- 어깨 너비의 화면상 크기가 달라진다.
- 머리-어깨·어깨-골반의 세로 비율이 시점에 따라 달라진다.
- 실제 앞으로 숙인 동작과 카메라 시점 변화가 분리되지 않을 수 있다.

정면 카메라를 측면 카메라로 옮기는 경우는 현재 구현의 안정적인 지원 범위를 벗어난다.

## 우선 개발안

### 1. 신체 좌표 정규화 강화

- 어깨 중심을 원점으로 사용한다.
- 어깨 너비를 가로 스케일로 사용한다.
- 어깨선 방향을 기준으로 랜드마크를 회전시켜 카메라 롤의 영향을 줄인다.
- 카메라 이동으로 인한 전체 평행이동과 실제 신체 변형을 분리한다.

### 2. 카메라 이동 중 반응 일시정지

모든 랜드마크가 짧은 시간에 같은 방향으로 크게 이동하거나 화면 크기가 급변하면 카메라 이동으로 간주한다.

1. 카메라 이동 상태를 감지한다.
2. 약 `0.5~1.0초` 동안 자세 이벤트와 로봇 개입을 중지한다.
3. 새 화면에서 사람의 랜드마크가 안정되면 판정을 재개한다.
4. 안정화 직후의 나쁜 자세를 이전 프레임의 연속 자세로 이어 붙이지 않는다.

카메라가 이동하는 순간 로봇이 잘못된 과장 포즈를 호출하지 않도록 하는 안전 기능이다.

### 3. 자동 기준값 갱신

수동 캘리브레이션 대신 다음 조건에서만 기준값을 천천히 갱신한다.

- 사람이 화면에 충분히 보인다.
- 현재 자세가 `good`이다.
- 카메라 이동 상태가 아니다.
- 일정 시간 동안 랜드마크가 안정적이다.

나쁜 자세가 시작되면 기준값을 고정한다. 그렇지 않으면 사용자가 나쁜 자세를 취하는 동안 기준값이 따라가서 나쁜 자세를 정상으로 학습할 수 있다.

## 측면 시점까지 지원하려면

정면에서 측면으로 크게 이동하는 경우에는 2D 화면 좌표만으로는 한계가 있다. 다음 중 하나가 필요하다.

- MediaPipe `world landmarks`를 이용한 3D 상대 벡터 계산
- 깊이 카메라 또는 RGB-D 센서
- 정면·측면 카메라 두 대를 사용하는 멀티뷰 구성

현재 코드에서는 `extract_angles()`가 `world` 인자를 받지만 실제 계산에는 사용하지 않는다. 측면 시점 지원 작업에서는 이 부분부터 검증해야 한다.

## 검증 시나리오

다음 영상을 각각 별도로 촬영하고, 사람이 정상 자세일 때 오검출이 발생하는지와 나쁜 자세 트리거가 유지되는지를 비교한다.

1. 정면 고정 카메라
2. 정면에서 카메라를 20~30cm 앞뒤로 이동
3. 카메라를 좌우로 20~30cm 이동하되 사용자를 계속 정면으로 촬영
4. 카메라를 좌우로 이동해 비스듬한 시점으로 촬영
5. 정면에서 측면으로 전환

각 시나리오에서 기록할 항목:

- `torso_pitch_deg`
- `neck_pitch_deg`
- `lateral_tilt_deg`
- `good / bad / unknown` 판정
- 카메라 이동 중 개입 발생 여부
- 이동 후 안정화까지 걸린 시간

## 구현 우선순위

1. 카메라 이동 감지 후 개입 일시정지
2. 신체 중심·어깨선 기준 정규화
3. 정상 자세 구간의 자동 기준값 갱신
4. `world landmarks` 기반 측면 시점 계산 검증
5. 필요할 때 깊이 카메라 또는 멀티뷰 도입

현재 프로토타입에서는 카메라를 정면에 두고 앞뒤 거리만 바꾸는 실험부터 시작하는 것이 가장 안정적이다.
