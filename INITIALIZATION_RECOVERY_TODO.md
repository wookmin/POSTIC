# 초기화 및 초기 복구 예정 작업

현재 `minwook` 브랜치는 `45164e6` 기준의 자세 반응 프로토타입 상태를 유지한다.
초기화와 복구 기능은 메인 동작에 반영하지 않고, 아래 검증이 끝난 뒤 별도 작업으로
추가한다.

## 현재 보류 이유

- 초기 시작 자세에서 목 모터 ID 9가 과부하 오류 `0x20`을 발생시켰다.
- ID 1·4·5도 설정된 기계적 위치 한계를 벗어난 상태에서 움직임이 멈춘 사례가 있다.
- ID 2·3이 실제로 구조 하중을 함께 지지하는지 아직 확정하지 않았다.
- ID 2·3은 현재 위치 제어가 아닌 속도 제어 또는 유휴 축으로 설정되어 있다.
- 현재 zero position과 실제로 안전하게 세워진 자세의 관계를 실물에서 다시 확인해야 한다.

## 추후 구현 방향

1. 모터별 실제 구동축과 하중 지지축을 확인한다.
2. ID 2·3을 포함할지 결정하고, 포함한다면 위치 제어 모드·zero·한계값을 개별 검증한다.
3. 초기화 전 모든 모터의 동작 모드, 토크, 하드웨어 에러, 현재 위치를 점검한다.
4. 범위 밖 자세의 자동 복구는 여러 모터를 동시에 움직이지 않고 한 축씩 시험한다.
5. 첫 목표는 기계적 한계보다 안쪽으로 두고, 위치 변화가 없거나 전류·하중 이상이 감지되면 즉시 중단한다.
6. zero position 도달 후에는 설정된 위치 한계를 변경하지 않은 상태로 유지한다.
7. 실제 하드웨어에서 단계별 테스트를 마친 뒤에만 메인 실행 경로와 연결한다.

## 테스트 원칙

- 초기화는 일반 자세 반응 실행과 분리된 명시적 명령으로 시작한다.
- 카메라 인식 전에 모터 목표를 자동으로 zero position에 쓰지 않는다.
- 과부하 오류를 반복해서 재부팅하거나 해제한 뒤 재시도하지 않는다.
- 모든 실험은 로봇을 지지하고 비상 전원을 차단할 수 있는 상태에서 수행한다.
