# 직접 의존성만 고정한다. 전이 의존성은 pip 가 해결한다.
# 전체 스냅샷이 필요하면: pip freeze > requirements.lock.txt
#
# 설치:
#   python3 -m venv ~/dynamixel-venv        # 이미 있으면 생략
#   ~/dynamixel-venv/bin/pip install -r requirements.txt
#
# 이 프로젝트는 카메라 · 마이크 · 모터를 한 프로세스에서 다룬다.
# ROS2 의 dynamixel_sdk 에 의존하지 않으므로 setup.bash 를 source 할 필요가 없다.

# 카메라 / 자세 인식
mediapipe==1.0.0
opencv-python==5.0.0.93
numpy==2.2.6

# 마이크 / 음성
vosk==0.3.45
sounddevice==0.5.5

# Gemini
google-genai==2.17.0
pydantic==2.13.4
python-dotenv==1.2.2

# 설정
PyYAML==6.0.3

# 모터
dynamixel-sdk==4.0.5
pyserial==3.5
